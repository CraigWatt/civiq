{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "dist",
    "rootDir": ".",
    "composite": true,
    "types": [
      "node"
    ]
  },
  "include": [
    "bin/**/*.ts",
    "envs/**/*.ts",
    "lib/**/*.ts"
  ]
}

